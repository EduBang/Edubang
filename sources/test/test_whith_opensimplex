# import pygame as pg
# from sympy import vector
# from opensimplex import OpenSimplex

# pg.init()
# pg.display.set_caption("Test Rendering Planets")
# screen = pg.display.set_mode((1000, 1000))  # Initialiser la fenêtre d'affichage

# def draw_circle(display: pg.Surface, radius: int, position: vector) -> None:
#     for x in range(-radius, radius, 1):
#         for y in range(-radius, radius, 1):
#             if (x * x) + (y * y) <= radius * radius:
#                 display.set_at((position.x + x, position.y + y), (255, 0, 0))

# def main():
#     clock = pg.time.Clock()
#     running = True

#     while running:
#         for event in pg.event.get():
#             if event.type == pg.QUIT:
#                 running = False

#         screen.fill((0, 0, 0))
#         draw_circle(screen, 100, (500, 500))
#         pg.display.flip()
#         clock.tick(60)  # Cap the frame rate at 60 FPS

#     pg.quit()

# main()